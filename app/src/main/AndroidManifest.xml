<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
          package="com.example.trialapplication">

    <application
            android:allowBackup="true"
            android:icon="@mipmap/ic_launcher"
            android:label="@string/app_name"
            android:roundIcon="@mipmap/ic_launcher_round"
            android:supportsRtl="true"
            android:theme="@style/AppTheme">
        <activity android:name=".SplashScreenActivity"
                  android:theme="@style/AppTheme.FullScreen">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
        <activity android:name=".MainActivity" />
        <activity android:name=".SplashScreenTextActivity" android:theme="@style/AppTheme.FullScreen"/>
        <activity android:name=".Slider" android:theme="@style/AppTheme.FullScreen"/>
        <activity android:name=".Slider_other" android:theme="@style/AppTheme.FullScreen"/>
        <activity android:name=".Slider_other_again" android:theme="@style/AppTheme.FullScreen"/>
        <activity android:name=".InfiSlider" android:theme="@style/AppTheme.FullScreen"/>
        <activity android:name=".SliderOwn" android:theme="@style/AppTheme.NoActionBar"/>
        <activity android:name=".SignUp" android:theme="@style/AppTheme.NoActionBar"/>
        <activity android:name=".VerificationPage" android:theme="@style/AppTheme.NoActionBar"/>
        <activity android:name=".HomePage" android:theme="@style/AppTheme.FullScreen"/>

    </application>

</manifest>